---
ticket: HEDS25-580
validated: yes
---

# Plan d’implémentation

Le périmètre et les garde-fous ont été validés par l’utilisateur dans la demande de traiter les tickets dans l’ordre. Ce ticket reste strictement non destructif.

- [x] Exporter le schéma `public` live sans données dans `supabase/baseline/`.
  - Fichiers : `scripts/supabase/export-schema-baseline.ps1`, `supabase/baseline/public-schema.sql`, `supabase/baseline/catalog.json`, `supabase/baseline/stack.json`.
  - Autorisé : `pg_dump --schema-only`, lectures catalogue, inventaire Docker.
  - Interdit : DDL/DML live, données métier, secrets, propriétaires, commentaires.
  - Validation : JSON parsable, dump sans `COPY`/`INSERT`/`UPDATE`/`DELETE`, empreintes cohérentes.
  - Rollback : suppression des seuls artefacts versionnés ; aucun rollback base nécessaire.

- [x] Geler les anciennes lignées et imposer le répertoire canonique.
  - Fichiers : `supabase/baseline/legacy-sql-manifest.json`, `scripts/supabase/schema-baseline-lib.mjs`, `scripts/supabase/check-schema-baseline.mjs`.
  - Autorisé : enregistrer les empreintes des SQL historiques existants.
  - Interdit : déplacer, modifier ou rejouer ces scripts.
  - Validation : tout nouveau SQL sous `migrations/`, `backend/supabase/migrations/` ou `sql/` échoue ; toute modification d’un fichier gelé échoue.

- [x] Bloquer les opérations destructrices non approuvées.
  - Fichiers : `supabase/schema-guard-allowlist.json`, contrôle Node.
  - Détection : `DROP` d’objet, `TRUNCATE`, désactivation RLS, DML destructif.
  - Les exceptions historiques sont liées à une empreinte et une justification ; une modification invalide l’exception.
  - Validation : tests positifs et négatifs ciblés.

- [x] Rendre le contrôle obligatoire localement et en CI.
  - Fichiers : `package.json`, `.github/workflows/deploy-prod.yml`, tests unitaires.
  - Commandes : `npm run db:schema:check`, tests unitaires, build existant.
  - Sécurité : aucune clé requise en CI pour vérifier l’artefact versionné.

- [x] Vérifier la reproductibilité sans toucher à la production.
  - Vérification structurelle automatique du dump et du catalogue.
  - Vérification isolée de restauration lorsque la stack PostgreSQL Supabase éphémère est disponible.
- La production ne reçoit aucune migration dans ce ticket.

## Preuves de validation

- `npm run db:schema:check` : réussi.
- Tests unitaires ciblés : 5/5 réussis, dont les contournements destructifs, append-only et dérive du catalogue.
- Suite Vitest complète : 81 fichiers et 1 091 tests réussis.
- Tests de sécurité backend : 31/31 réussis.
- Restauration isolée : 117 relations, 258 politiques RLS et 42 triggers restaurés dans un conteneur éphémère supprimé après contrôle.
- Aucun DDL/DML n'a été exécuté sur la base de production.

## Couverture des critères Jira

- Catalogue/DDL/policies/grants/triggers/dépendances : dump + catalogue JSON.
- Comparaison live/dépôt : export reproductible + empreintes + commande de contrôle.
- Objets live non versionnés : catalogue de baseline servant de référence qualifiée.
- Reconstruction : artefact de bootstrap sans données et vérification isolée.
- CI : répertoire canonique, lignées gelées et garde destructive.
- Schémas exposés/interne : `stack.json` enregistre `PGRST_DB_SCHEMAS=public` et sépare les schémas internes.
- Stack self-hosted : versions PostgreSQL, gateway et images inventoriées sans mise à niveau automatique.
