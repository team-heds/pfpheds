# Revue HEDS25-579

## Verdict final

La première revue a bloqué la livraison parce que les erreurs renvoyées dans `result.error` par Supabase n'étaient pas propagées et que la structure des exports Excel n'était pas verrouillée par des tests.

Ces deux points ont été corrigés puis revus une seconde fois :

- les neuf lectures Supabase requises sont contrôlées avant toute projection ;
- une erreur empêche l'affichage de données partielles et propose une nouvelle tentative ;
- le schéma des 18 colonnes PFP et les positions des groupes de la vue d'ensemble sont centralisés et testés ;
- « Répondant HES » et « Formateur·trice HES » restent deux champs distincts ;
- la source reste l'annuaire étudiant existant, sans requête supplémentaire par étudiant.

## Contrôles

- Tests ciblés : 9/9 réussis.
- Suite Vitest : 80 fichiers, 1 087 tests réussis.
- Tests de présentation : 14/14 réussis.
- ESLint ciblé : réussi.
- Build Vite et service worker PWA : réussi.

Max severity: none
Ship allowed: yes
