---
ticket: HEDS25-588
validated: yes
---

# Plan d’implémentation

- [x] **1. Centraliser le contrat et le moteur de filtrage**
  - Objectif : ajouter un service pur pour normaliser les filtres, rechercher les institutions et vérifier qu’une même place satisfait les critères, langues et PFP actifs.
  - Fichiers attendus : `src/service/institutionFiltersService.js`, `tests/unit/institutionFilters.spec.js`.
  - Autorisé : lecture des colonnes existantes des objets institutions/places et fonctions pures.
  - Interdit : requêtes supplémentaires, migration, écriture Supabase, modification RLS.
  - Tests : recherche accentuée/insensible à la casse, identifiants texte/nombre, institution masquée, OU intrafamille, ET interfamilles, offre PFP vide/non vide, plusieurs places.
  - Validation : `npm run test:unit -- --run tests/unit/institutionFilters.spec.js`.
  - Sécurité/rollback : aucune donnée écrite; rollback par suppression du service et retour aux calculs locaux.

- [x] **2. Unifier l’interface et l’état des filtres**
  - Objectif : faire de `FilterSidebar.vue` l’unique panneau utilisé par les deux pages, avec recherche, nombre de résultats, filtres actifs et action de réinitialisation.
  - Fichiers attendus : `src/components/common/filters/FilterSidebar.vue`, éventuellement un test de composant ciblé.
  - Autorisé : props/événements, libellés, styles responsive et accessibilité clavier.
  - Interdit : modifier les sidebars sociales, la navbar ou les filtres admin.
  - Tests : émissions identiques, reset complet, libellés accessibles.
  - Validation : test Vitest ciblé et vérification manuelle desktop/tablette/mobile.
  - Sécurité/rollback : composant local aux deux routes; retour possible à la version précédente sans impact serveur.

- [x] **3. Brancher la liste et la carte sur la logique partagée**
  - Objectif : supprimer les deux implémentations divergentes, utiliser les mêmes données et règles, afficher le même résumé et fiabiliser l’initialisation Leaflet avec ou sans cache Pinia.
  - Fichiers attendus : `src/views/institutions/Institution.vue`, `src/components/common/filters/FiltreMap.vue`.
  - Autorisé : suppression des imports `filter.json` sur ces deux écrans, gestion locale de Leaflet, résumé de résultats.
  - Interdit : changer les routes, les stores, les pages admin ou les données.
  - Tests : liste et carte retournent les mêmes identifiants pour un même état; carte limitée aux coordonnées valides; initialisation avec données préchargées.
  - Validation : tests ciblés puis `npm run build`.
  - Sécurité/rollback : aucun changement de permissions; rollback limité aux deux composants.

- [x] **4. Vérifier le ticket de bout en bout**
  - Objectif : contrôler chaque critère d’acceptation et inspecter le diff final.
  - Fichiers attendus : mise à jour des cases de ce plan uniquement.
  - Autorisé : tests, build et contrôle visuel local.
  - Interdit : déploiement ou fusion automatique.
  - Tests : `npm run test:unit -- --run tests/unit/institutionFilters.spec.js`, tests de composant ajoutés, `npm run build`.
  - Validation : comparaison manuelle de `/institution` et `/map` avec les mêmes combinaisons sur desktop, tablette et mobile.
  - Sécurité/rollback : confirmer qu’aucun fichier Supabase/backend n’est modifié et qu’aucune donnée de production n’est écrite.

## Correspondance avec les critères d’acceptation

- Moteur et composant uniques : tâches 1 à 3.
- Recherche, canton, critères, langues et PFP identiques : tâches 1 à 3.
- Données Supabase réelles, sans dépendance métier à `filter.json` : tâches 1 et 3.
- Sémantique d’une même place, OU dans une famille et ET entre familles : tâche 1.
- Institutions masquées et coordonnées valides : tâches 1 et 3.
- Résultats, filtres actifs et réinitialisation : tâche 2.
- Tests automatisés et responsive : tâches 1, 2 et 4.

## Risques structurels

- Migration/RLS/storage/routes publiques/IA/dépendances : aucun changement prévu.
- Destruction de données : aucune opération prévue.
- Déploiement : explicitement exclu de ce plan.
