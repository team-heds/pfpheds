---
ticket: HEDS25-512
validated: yes
implemented: yes
---

# Plan dâ€™implÃ©mentation â€” envoi sÃ©curisÃ© du lien de rÃ©initialisation

## TÃ¢che 1 â€” Ajouter une route publique backend Ã  rÃ©ponse uniforme

- [x] CrÃ©er une route `POST /api/auth/password-recovery`, montÃ©e avant lâ€™authentification globale dans `backend/index.js`.
- [x] Valider et normaliser lâ€™email sans le journaliser, imposer une limite IP dÃ©diÃ©e et ignorer toute URL de redirection fournie par le client.
- [x] Appeler `supabase.auth.resetPasswordForEmail()` avec la redirection serveur de production.
- [x] RÃ©pondre avec le mÃªme statut `202` et le mÃªme corps pour une adresse connue, inconnue ou un Ã©chec GoTrue ; journaliser seulement un identifiant de corrÃ©lation, une catÃ©gorie et un statut non sensible.

**Fichiers attendus**

- nouveau `backend/supabase/passwordRecoveryRequestBackend.js`
- `backend/index.js`
- nouveau `backend/test/passwordRecoveryRequest.test.js`
- `backend/.env.production.example`

**Interdit**

- aucune table de jetons ;
- aucun `service_role`, email, code, OTP, URL complÃ¨te ou message SMTP dans la rÃ©ponse ou les logs ;
- aucune ouverture des autres routes `/api` anonymes.

**Validation**

- adresse connue/inconnue : rÃ©ponse strictement identique ;
- erreur GoTrue : rÃ©ponse identique et Ã©vÃ©nement expurgÃ© ;
- adresse invalide : rÃ©ponse contrÃ´lÃ©e sans appel GoTrue ;
- limite IP et `X-Forwarded-For` derriÃ¨re un seul proxy ;
- `npm --prefix backend test`.

**Retour arriÃ¨re**

Retirer uniquement le routeur public et restaurer lâ€™appel direct actuel ; aucune donnÃ©e nâ€™est migrÃ©e.

## TÃ¢che 2 â€” Centraliser la demande cÃ´tÃ© frontend

- [x] Ajouter un service public utilisant la base API dÃ©jÃ  rÃ©solue par `src/service/apiClient.js`, sans chercher de session JWT.
- [x] Remplacer lâ€™appel direct Ã  GoTrue dans `authStore.resetPasswordSupabase()`.
- [x] Afficher une confirmation gÃ©nÃ©rique stable et ne plus Ã©crire lâ€™objet dâ€™erreur brut dans la console.
- [x] Conserver la garde anti-double-clic comme protection UX, sans la prÃ©senter comme contrÃ´le de sÃ©curitÃ©.

**Fichiers attendus**

- nouveau `src/service/passwordRecoveryRequestService.js`
- `src/service/apiClient.js`
- `src/stores/authStore.js`
- `src/views/auth/LoginHome.vue`
- tests unitaires associÃ©s dans `tests/unit/`

**Validation**

- URL backend correcte en local et production ;
- aucun header dâ€™authentification requis ;
- confirmation identique pour tous les rÃ©sultats publics ;
- double clic bloquÃ© ;
- tests Vitest ciblÃ©s et tests Playwright de HEDS25-515.

**Retour arriÃ¨re**

Restaurer lâ€™appel `resetPasswordForEmail()` dans le store sans toucher Ã  GoTrue.

## TÃ¢che 3 â€” Garantir le montage de la page de rÃ©cupÃ©ration

- [x] Dans `src/main.js`, traiter `/reset-password` et `/new-password` comme routes publiques de rÃ©cupÃ©ration qui nâ€™attendent pas le chargement du profil dâ€™une session existante.
- [x] Continuer Ã  attendre le routeur avant le montage et conserver lâ€™initialisation complÃ¨te pour toutes les autres pages.
- [x] Ajouter un test de rÃ©gression simulant une initialisation Auth qui ne se rÃ©sout pas.

**Fichiers attendus**

- `src/main.js`
- nouveau test unitaire de bootstrap public dans `tests/unit/`

**Interdit**

- ne pas dÃ©sactiver lâ€™authentification globale ;
- ne pas rendre accessibles les routes mÃ©tier ;
- ne pas accepter une session ordinaire comme preuve de rÃ©cupÃ©ration.

**Validation**

- `/reset-password` se monte malgrÃ© une session/profil bloquÃ© ;
- une route protÃ©gÃ©e conserve le bootstrap complet ;
- aucune page blanche sur lien expirÃ©.

**Retour arriÃ¨re**

Restaurer le bootstrap unique actuel ; aucun Ã©tat serveur nâ€™est modifiÃ©.

## TÃ¢che 4 â€” Versionner le courriel et les redirections GoTrue

- [x] Ajouter un modÃ¨le HTML statique HEdS contenant uniquement `{{ .ConfirmationURL }}` comme cible du bouton et un texte indiquant expiration, usage unique et possibilitÃ© dâ€™ignorer la demande.
- [x] Ajouter dans lâ€™override GoTrue le sujet, lâ€™URL du modÃ¨le, `GOTRUE_SITE_URL` et une allow-list limitÃ©e aux routes de rÃ©cupÃ©ration HEdS.
- [x] Ã‰tendre le test statique de dÃ©ploiement pour vÃ©rifier ces valeurs sans secret.
- [x] Mettre Ã  jour la documentation Auth et la recette.

**Fichiers attendus**

- nouveau `public/auth-email-templates/password-recovery.html`
- `backend/deployment/supabase-auth-security.override.yml`
- `tests/unit/supabaseAuthDeployment.spec.js`
- `documentation/docs/auth/overview.md`
- `documentation/docs/testing/password-recovery-recette.md`

**SÃ©curitÃ©**

Le modÃ¨le ne contient jamais de jeton en dur. GoTrue remplace `ConfirmationURL` au moment de lâ€™envoi. Les paramÃ¨tres SMTP restent exclusivement dans lâ€™environnement du VPS.

**Validation**

- build frontend et documentation ;
- contrÃ´le statique du modÃ¨le et de lâ€™override ;
- validation Compose avant redÃ©marrage Auth ;
- aprÃ¨s autorisation de dÃ©ploiement : healthchecks, email rÃ©el, redirection, audit agrÃ©gÃ© et absence de secret dans les logs.

**Retour arriÃ¨re**

Retirer les variables de modÃ¨le/sujet pour revenir au modÃ¨le GoTrue prÃ©cÃ©dent, puis recrÃ©er uniquement le service Auth.

## Correspondance avec les critÃ¨res dâ€™acceptation

| CritÃ¨re | Couverture |
| --- | --- |
| Une demande valide produit un jeton sÃ©curisÃ© | GoTrue reste lâ€™unique gÃ©nÃ©rateur via la route backend |
| Le lien cible le bon environnement et le bon Ã©cran | URL serveur fixe, `SITE_URL`, allow-list et test du bootstrap public |
| Le courriel est envoyÃ© avec un contenu validÃ© | ModÃ¨le et sujet versionnÃ©s, recette rÃ©elle |
| Adresse connue/inconnue indistinguables | RÃ©ponse `202` uniforme et tests backend/frontend |
| Aucun secret dans les journaux | Journal structurÃ© expurgÃ© et tests nÃ©gatifs |
| Les erreurs dâ€™envoi sont suivies | CorrÃ©lation serveur sans PII et contrÃ´le des logs |

## Commandes de validation prÃ©vues

```bash
npm --prefix backend test
npm run test:unit -- --run tests/unit/authStore.spec.js tests/unit/passwordRecoveryRequestService.spec.js tests/unit/supabaseAuthDeployment.spec.js
npx playwright test tests/e2e/password-recovery.spec.js --project=chromium
npm run build
npm run docs:build
```

## Validation humaine requise

Ce plan doit Ãªtre explicitement validÃ© avant toute modification du code applicatif, de la route publique ou de la configuration GoTrue.

Plan validÃ© explicitement par lâ€™utilisateur le 2026-08-08.
