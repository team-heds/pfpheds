# HEDS25-579 — Répondant HES dans la vue d’ensemble FP

## Trace vérifiée

- `backend/supabase/audienceDirectoryBackend.js` lit déjà `StudentsPhysio.repondant_hes` par `user_id` et l’expose dans `/api/audiences/students`.
- La route annuaire est authentifiée et autorisée côté serveur; aucune nouvelle exposition Supabase ou migration n’est nécessaire.
- `src/service/studentDirectoryService.js` conserve le champ dans son cache partagé de cinq minutes.
- La perte se produit uniquement dans `VueDEnsembleFP.vue`, lors de la projection des étudiants vers les lignes du tableau.

## Intégration retenue

- Propager la source canonique dans les lignes étudiantes et les lignes PFP aplaties.
- Ajouter une colonne distincte, triable, avec `—` en absence de valeur.
- Inclure le répondant dans la recherche et dans les exports « PFP » et « Vue d’ensemble FP ».
- Conserver « Répondant HES » et « Formateur·trice HES » comme deux colonnes distinctes.
- Invalider le cache annuaire uniquement lors d’un clic explicite sur « Actualiser ».
- Afficher une erreur utile si l’annuaire ou les données de stages ne peuvent pas être chargés.

## Risques maîtrisés

- Aucun appel par étudiant : une seule lecture de l’annuaire existant.
- Les offsets Excel sont ajustés avec l’ajout de la colonne de base.
- Les filtres PFP/statut/type historiques de l’export ne sont pas refondus dans ce ticket.
